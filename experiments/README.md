The scripts here run combinations of algorithms (IRIS and UMAP) and hyperparameters. They will
produce layouts and time files in `../data/outputs/`

- `run-all.py`: runs all layouts used for the paper. Add `--force` to overwrite existing layouts in `../data/outputs/`